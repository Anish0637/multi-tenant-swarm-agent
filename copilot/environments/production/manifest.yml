# copilot/environments/production/manifest.yml
# Run: copilot env init --name production

name: production
type: Environment

network:
  vpc:
    cidr: "10.0.0.0/16"
    subnets:
      public:
        - cidr: "10.0.1.0/24"
        - cidr: "10.0.2.0/24"
      private:
        - cidr: "10.0.11.0/24"
        - cidr: "10.0.12.0/24"
